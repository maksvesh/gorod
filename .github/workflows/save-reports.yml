name: Save Game Results

on:
  workflow_dispatch:    # Ручной запуск из интерфейса
  repository_dispatch:  # Запуск через API
    types: [game_results]
  push:                 # Запуск при пуше в main
    branches: [ main, master ]
  schedule:
    - cron: '0 0 * * *' # Ежедневно в полночь

jobs:
  save-results:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITO }}
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Create results directory
      run: mkdir -p game-results
        
    - name: Process game results
      env:
        GAME_DATA: ${{ toJson(github.event.client_payload.game_data) }}
        COMMIT_MESSAGE: ${{ github.event.client_payload.commit_message }}
        PLAYER_NAME: ${{ github.event.client_payload.player_name }}
      run: |
        if [ -n "$GAME_DATA" ]; then
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          FILENAME="result_${TIMESTAMP}_${PLAYER_NAME}.json"
          echo "$GAME_DATA" > "game-results/$FILENAME"
          echo "✅ Результат сохранен: $FILENAME"
        else
          echo "⚠️ Нет данных для сохранения"
          # Создаем тестовый файл при push/schedule
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          echo '{"test": "auto_generated", "timestamp": "'$TIMESTAMP'"}' > "game-results/test_$TIMESTAMP.json"
        fi
        
    - name: Commit and push results
      run: |
        git config --local user.email "maksvesh@yandex.ru"
        git config --local user.name "GITO"
        git add game-results/
        git diff --staged --quiet || git commit -m "🎮 Добавлены результаты игры [automated]"
        git push
